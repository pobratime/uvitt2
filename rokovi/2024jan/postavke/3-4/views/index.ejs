<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

</head>

<body>
  <div class="container">
    <h1 class="my-4">Deda Mrazova lista</h1>
    <div class="d-flex flex-column flex-md-row justify-content-between gap-5">
      <div class="border border-success rounded flex-fill">
        <h2 class="ms-5 mt-2"> Dobra deca</h2>
        <% for(let i=0; i < dela.length; i++) { %>
          <% if(dela[i].dobro) { %>
            <div class="d-flex flex-row justify-content-between ms-5 me-5">
              <span>
                <a href="kid/<%= encodeURIComponent(dela[i]._id) %>">
                <%= dela[i].ime %>
                </a>
              </span>
              <span>
                <%= dela[i].poeni %>
              </span>
            </div>
            <hr class="ms-5 me-5">
            <% } %>
              <% }%>
      </div>
      <div class="border border-danger rounded flex-fill">
        <h2 class="ms-5 mt-2">Losa Deca</h2>
        <% for(let i=0; i < dela.length; i++) { %>
          <% if(!dela[i].dobro) { %>
            <div class="d-flex flex-row justify-content-between ms-5 me-5">
              <span>
                <a href="kid/<%= encodeURIComponent(dela[i]._id)%>">
                <%= dela[i].ime %>
                </a>
              </span>
              <span>
                <%= dela[i].poeni %>
              </span>
            </div>
            <hr class="ms-5 me-5">
            <% } %>
              <% }%>

      </div>
    </div>
    <div class="d-flex flex-column justify-content-between">
      <h2 class="mt-5"> Novo delo</h2>
      <form class="d-flex flex-column gap-2" method="post">
        <span>Dete:</span>
        <div>
          <input type="text" name="imeProsldjeno" class="form-control">
        </div> 
        <div>
          <span>Sta je uradio?</span>
          <br>
          <input type="text" name="deloProsledjeno" class="form-control">
        </div>
        <div>
          <span> Poeni: </span>
          <br>
          <input type="number" name="poeniProsledjeno" class="form-control">
        </div>
        <div class="mt-2">
          <button type="submit" class="btn btn-primary form-control"> unesi</button>
        </div>
      </form>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js">
  </script>
</body>

</html>
