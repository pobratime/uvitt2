<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body style="margin-left:10%; margin-right: 10%;">
  <h1 class="h1">  <%= dela[0].ime %></h1>
  <!-- MAIN DIV -->
  <div class="d-flex justift-content-evenly gap-5">
    <!-- SLIKA -->
    <div class="w-25">
      <% if(dela[0].dobro) { %>
        <img src="/good.png" class="w-100">
      <% } else { %>
        <img src="/bad.png" class="w-100">
      <% } %>
    </div>
    <!-- DELO -->
     <div class="flex-fill">
      <h2> Delo </h2>
      <% if(dela[0].dobro) { %>
        <span class="text-success"> <%= dela[0].delo %></span>
      <% } else { %>
        <span class="text-danger"> <%= dela[0].delo %></span>
      <% } %>
     </div>
    <!-- PISMO -->
     <div class="flex-fill">
      <% if(dela[0].pismo != "") { %>
        <h2> Pismo </h2>
        <p> <%= dela[0].pismo %> </p>
      <% } else { %>
        <p> Nema pisma! </p>
      <% } %>
     </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
